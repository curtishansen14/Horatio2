@model IEnumerable<Horatio_2._0.Models.UserLabor>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AspNetUser.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Labor.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserQuest.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Target)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isComplete)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AspNetUser.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Labor.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserQuest.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Target)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isComplete)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.UserLaborID }) |
                @Html.ActionLink("Details", "Details", new { id=item.UserLaborID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.UserLaborID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
